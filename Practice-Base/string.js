/*const name = 'Ильсур'
const age = 20

function getAge() {
	return age
}

const output = `Привет! Меня зовут ${name}, мой возраст ${getAge()} лет` // `` - для такого синтаксиса
console.log(output)

const output `
	<div>vvv</div>
`
console.log(output)*/

/*const name = 'Ильсур'
console.log(name.length)
console.log(name.toUpperCase()) // Верхний регистр у всей строки
console.log(name.toLowerCase()) 

console.log(name.charAt(2)) // index (ь)

console.log(name.indexOf('И')) // index (0)
console.log(name.startsWith('Иль')) // true or false
console.log(name.endsWith('сур')) // true or false
console.log(name.repeat(3)) // Dublicate 

const string = '   pasport   '
console.log(string.trim()) // чистит пробелы
console.log(string.trimLeft()) // чистит пробелы
console.log(string.trimRight()) // чистит пробелы*/

/*function logPerson(s, name, age) { // JS разделяет строку на массив
	
	return `${s[0]}${name}${s[1]}`
}

const personName = 'Ilsur'
const personAge = 20

const output = logPerson`Имя: ${personName}, Возраст: ${personAge}` // new syntaxis 
console.log(output)*/

